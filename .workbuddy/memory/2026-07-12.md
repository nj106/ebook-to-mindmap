# 2026-07-12

## 思维导图卡片：新增「复制为纯文本大纲」按钮（最终版）
- 需求澄清：要的是**独立的按钮放在下载下拉隔壁**（不是分隔式按钮组），且复制的是**真正的 plaintext 大纲格式**（不是 `getDataString()` 的 JSON 文本）。
- 新建独立组件 `src/components/CopyMindMapPlaintextButton.tsx`：通过 `mindElixirRef` 取 `instance`，调用 `instance.getData()` 得到 `MindElixirData`，再用 `mind-elixir/plaintextConverter` 的 `mindElixirToPlaintext(data)` 转成纯文本大纲，写入剪贴板 + sonner 提示。
- 在 `src/components/MindMapCard.tsx` 中把该按钮放在 `DownloadMindMapButton` 前面（用 Fragment 包裹），两者是相邻的独立按钮。
- `DownloadMindMapButton.tsx` 已还原为原始下拉（之前误改的按钮组已回退）。
- i18n：`download` 命名空间新增 `copyPlaintext`（tooltip）与 `copiedPlaintext`（成功提示）；已删除上一版误加的 `copyMindElixirText`/`copiedMindElixirText`。
- 关键知识点：plaintext 大纲格式规范见 `.claude/skills/mind-elixir-plaintext-format/SKILL.md`（2 空格缩进、`- 节点`、`[^id]`、样式、summary、link 等）。转换 API：`mindElixirToPlaintext(data)` / `plaintextToMindElixir(text)`，从 `mind-elixir/plaintextConverter` 导入。实例数据方法用 `getData()`（返回 MindElixirData）。mind-elixir 版本 5.10.0-beta.1。
- 已通过 `tsc -b` 类型检查。
